//> using scala "3.3.1"
//> using lib "com.lihaoyi::cask:0.8.3"
//> using lib "com.lihaoyi::scalatags:0.12.0"
/**
 * Cask App
 * SDK for Minecraft versions info
 *
 * OpenAPI spec version: 1.3.5-pre.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */


// this is generated from apiRoutes.mustache
package org.openapitools.server.api

import org.openapitools.server.model.*

import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*
import scala.util.Try

import org.openapitools.server.model.VersionManifest
import org.openapitools.server.model.VersionPackageInfo

class DefaultRoutes(service : DefaultService[Try]) extends cask.Routes {


        /** Get Minecraft version manifest
         * 
         */
        @cask.get("/mc/game/version_manifest.json")
        def getMinecraftVersionManifest(request: cask.Request) = {

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.getMinecraftVersionManifest())
            result <- Parsed.fromTry(resultTry)
        } yield result

        import VersionManifest.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : VersionManifest) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get Minecraft version package info
         * 
         */
        @cask.get("/v1/packages/:packageId/{versionId}.json")
        def getMinecraftVersionPackageInfo(packageId : String, request: cask.Request) = {

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            packageId <- Parsed(packageId)
            versionId <- Parsed(versionId)
            resultTry <- Parsed.eval(service.getMinecraftVersionPackageInfo(packageId, versionId))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import VersionPackageInfo.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : VersionPackageInfo) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }

    initialize()
}

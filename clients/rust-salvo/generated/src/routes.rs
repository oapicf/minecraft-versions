/*
 * salvo_openapi
 *
 * SDK for Minecraft versions info
 *
 * The version of the OpenAPI document: 1.3.5-pre.0
 *
 * Generated by: https://openapi-generator.tech
 */

//! API routing module
//!
//! This module defines the routing configuration for all API endpoints.

use salvo::prelude::*;
use crate::handlers::*;

/// Create the main router with all API routes
pub fn create_router() -> Router {
    let mut router = Router::new();

    // Routes for /mc/game/version_manifest.json
    router = router.push(
        Router::with_path("/mc/game/version_manifest.json")
            .push(
                Router::new()
                    .get(get_minecraft_version_manifest)
            )
    );
    // Routes for /v1/packages/{packageId}/{versionId}.json
    router = router.push(
        Router::with_path("/v1/packages/{packageId}/{versionId}.json")
            .push(
                Router::new()
                    .get(get_minecraft_version_package_info)
            )
    );

    router
}
